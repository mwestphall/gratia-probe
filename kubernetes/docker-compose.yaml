services:
  gratia-output:
    image: hub.opensciencegrid.org/osgpreview/kapel-gratia-output:latest
    build:
      context: .
      dockerfile: Dockerfile
    
    volumes:
      - /tmp/dirq/:/srv/kapel
      - ./sample.env:/etc/kapel-gratia-output/.env
      - ./gratia-config:/etc/kapel-gratia-output/gratia-config
